version: "3.8"
services:
    risk-db:
        container_name: risk-db
        image: mysql:latest
        environment:
            MYSQL_ROOT_PASSWORD: 'root'
            MYSQL_USER: 'admin'
            MYSQL_PASSWORD: 'admin'
            MYSQL_DATABASE: 'risk'
        ports:
            - 8765:3306
